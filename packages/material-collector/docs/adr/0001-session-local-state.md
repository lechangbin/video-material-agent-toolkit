# 使用会话级 SQLite，而非全局状态库

状态：accepted

CLI 进程保持无状态，调用方显式指定长期素材工作区，每次采集在其中创建独立会话状态目录并以 `session.sqlite3` 保存内部运行状态；媒体由素材工作区持有，会话只保存引用。这样既利用 SQLite 的事务、约束和恢复能力处理复杂采集流程，又避免全局状态库造成跨会话锁竞争、故障扩散和归档耦合；应用源码及安装目录不得产生运行期状态。
